import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  header: {
    height: 60,
    padding: 15,
    backgroundColor: 'lightgrey',
  },
  text: {
    color: 'white',
    fontSize: 30,
    textAlign: 'center',
  },
});
