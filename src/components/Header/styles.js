import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  header: {
    height: 50,
    padding: 15,
    backgroundColor: 'lightgrey',
  },
  text: {
    color: 'white',
    fontSize: 18,
    textAlign: 'center',
  },
});
